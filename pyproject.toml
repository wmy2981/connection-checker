[build-system]
requires = ["setuptools>=83"]
build-backend = "setuptools.build_meta"

[project]
name = "connection-checker"
version = "1.9.0.alpha.3"
description = "自托管网络连通性监控工具：Ping / TCP 端口 / HTTP 状态码检查，FastAPI + Vue3 仪表盘"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
  "fastapi>=0.115,<1",
  "uvicorn[standard]>=0.30",
  "pydantic>=2.7,<3",
  "pydantic-settings>=2.3",
  "ping3>=4.0",
  "httpx>=0.27,<1",
  "argon2-cffi>=23.1",
  "PyJWT>=2.8",
  "python-multipart>=0.0.9",
  "minio>=7.2,<8",
]

[project.optional-dependencies]
dev = [
  "pytest>=8",
  "pytest-asyncio>=0.23",
  "ruff>=0.5",
  "httpx>=0.27,<1",
]

[tool.setuptools.packages.find]
where = ["backend"]
include = ["app*"]

[tool.pytest.ini_options]
testpaths = ["backend/tests"]
asyncio_mode = "auto"
addopts = "-q"

[tool.ruff]
target-version = "py310"
line-length = 100
src = ["backend"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
